apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: flow-api
spec:
  tls:
  - hosts:
    - "api.akvo.org"
    secretName: tls
  rules:
    - host: "api.akvo.org"
      http:
        paths:
        - backend:
            serviceName: flow-api
            servicePort: 80
